# Lorus Bot

Телеграм-бот для заработка звёзд через выполнение заданий.

## Возможности
- Выполнение локальных и субграм-заданий
- Реферальная система
- Мини-игра для получения звёзд
- Админ-панель для управления заданиями и балансами

## Технологии
- Python 3.11 + aiogram 3.x
- PostgreSQL
- Linux deployment (systemd, Nginx)

## Запуск
```bash
pip install -r requirements.txt
python main.py

## Mini App
В проект добавлен Telegram Mini App на `aiohttp`.

### Переменные окружения
- `MINI_APP_URL` — публичный HTTPS URL Mini App (например, через Nginx/Cloudflare Tunnel)
- `MINI_APP_HOST` — хост локального сервера Mini App (по умолчанию `127.0.0.1`)
- `MINI_APP_PORT` — порт локального сервера Mini App (по умолчанию `8080`)

### Как это работает
- Бот поднимает Mini App сервер вместе с polling (`main.py`)
- В главном меню есть кнопка `📱 Mini App`
- Внутри Mini App кнопки отправляют команды обратно в бота через `Telegram.WebApp.sendData()`
